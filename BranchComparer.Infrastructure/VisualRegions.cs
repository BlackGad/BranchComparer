namespace BranchComparer.Infrastructure;

public static class VisualRegions
{
    public const string FILTER = nameof(FILTER);
    public const string STATUS = nameof(STATUS);
    public const string VISUALIZATION = nameof(VISUALIZATION);
}
